# ── 核心 ──────────────────────────────────────────────
requests>=2.31.0
beautifulsoup4>=4.12.0
lxml>=4.9.0
tqdm>=4.66.0
pandas>=2.1.0

# ── 数据采集 ──────────────────────────────────────────
datasets>=2.14.0          # HuggingFace datasets（FairCV）
selenium>=4.15.0          # JD 爬虫动态页面
webdriver-manager>=4.0.0  # 自动管理 ChromeDriver

# ── NLP / NER ─────────────────────────────────────────
spacy>=3.7.0
# 安装中文模型：python -m spacy download zh_core_web_sm
# 安装英文模型：python -m spacy download en_core_web_sm

# ── 嵌入消歧 ──────────────────────────────────────────
sentence-transformers>=2.3.0
numpy>=1.24.0
scikit-learn>=1.3.0       # DBSCAN 聚类

# ── LLM 调用（OpenAI 兼容接口，支持 DeepSeek）─────────
openai>=1.6.0

# ── 数据验证 ──────────────────────────────────────────
jsonschema>=4.20.0

# ── 语言检测 ──────────────────────────────────────────
langdetect>=1.0.9
