# data_engine 本地敏感配置模板
#
# 用法：
#   1. 把本文件复制为 data_engine/.env（同目录），不要直接改这个 .example
#   2. 把下面的 ghp_xxx 换成真实 token
#   3. 跑命令前 source 它：
#         set -a; source data_engine/.env; set +a
#         python3 -m data_engine run --sources github --limit-per-target 2
#
# .env 已被 data_engine/.gitignore 忽略，绝不会进入 git；不要把 token 放到 config.json。

# 任意只读 GitHub token（PAT classic 或 fine-grained 都行；不需要任何 scope）。
# 不设时 GitHub 走未认证速率（60/h），抓几次就被限流。
GITHUB_TOKEN=ghp_xxx
